---
task_id: InventorySystem
status: in_progress
assigned_to: Inventory Team（多 AI 协作）
progress: 90%
started: 2025-11-14
last_updated: 2025-11-22
---

# 任务状态

当前状态：in_progress
负责人：Inventory Team（多 AI 协作）
进度：90%

- 最后更新内容：
- Phase5 进行中：彻底移除 UWeaponDefinition 家族（文件已删除），`AShootWeaponActor` 仅保留表现字段（Mesh/MuzzleSocketName），CombatComponent/GA/HUD/拾取不再注入或访问 WeaponDefinition。`UShootHUDReticleComponent` 已改为通过 ItemDefinition 的 WeaponBasicConfig 取 ReticleClass，拾取/Quickbar/FInteractionQuery 缺失 include 已修复。
- Phase2-C 收尾：HUD 准星完全以 `UShootRangedWeaponInstance` 驱动并移除 Actor 回退；AnimNotify 显隐链路确认只经 `CombatComponent::HandleWeaponAnimNotify → UShootWeaponInstance::HandleVisualAnimCue`，GA 子类仍仅在内部通过 `GetSpawnedRangedActor()` 做表现；Reticle Widget 去掉 `AShootWeaponActor/AShootWeaponActor` 依赖，Spread/ADS 逻辑全部读取 WeaponInstance。
- Phase2-A 编译修复：`UShootGameplayAbility_Weapon_Fire::TraceBulletsInCartridge` 的声明/实现统一使用 `UShootRangedWeaponInstance*`，避免 GA 再与 `AShootWeaponActor` 强耦合；`AShootWeaponPickupActor` 补充 `Inventory/ShootInventoryItemInstance.h` 依赖以暴露 `EShootItemLifetime` 并直接通过 PlayerState 解析 InventoryManager；`AShootResourcePickup` 通过 `AShootPlayerState->GetInventoryManagerComponent()` 获取背包并移除失效的 `ShootInventoryFunctionLibrary` include；`UCombatComponent::ClearRuntimeSlots` 改用本地布尔恢复逻辑替代缺失的 `Misc/GuardValue.h`。
- Phase2-B QuickBar → WeaponInstance → HUD：`UCombatComponent` 增加 `GetOrCreateWeaponInstanceFromInventory`，`EquipItemInSlot/GetActiveWeaponInstance` 统一通过 `UShootWeaponInstance` 驱动；`UShootGA_Interact`（原 InteractCore）与 `UShootGA_Interaction_Collect` 负责 PressToInteract 拾取，Quickbar Widget 监听 `Msg_Weapon_AmmoChanged` 同步 Ammo/Reserve，`UShootHUDReticleComponent` 监听 Quickbar 槽位消息刷新当前准星，Legacy `AShootWeaponActor` 仅保留在表现层日志中，Implementation 指南同步记录遗留 Actor TODO。
- Phase2-A Runtime 捡枪/丢枪闭环验证：`UCombatComponent::DropActiveRuntimeWeapon`、`RemoveItemFromSlot` 与 `AShootWeaponPickupActor::HandlePickup` 增加 Server/Client 日志，覆盖 Hub Persistent→Runtime 流程、副本 RuntimeOnly 清理与 Listen Server 权威分流；保留 HUD Icon/GA_Interact TODO，待 Phase2-B/C 接手。
- Phase2 定义层落地（已废弃 WeaponDefinition）：`UShootInventoryItemDefinition_Weapon` 仅挂 Fragment（Basic/Ranged/Projectile）；`UShootEquipmentDefinition_Weapon` 默认 WeaponInstance 类型、Equip/Unequip Montage、ActorsToSpawn。EquipmentManager 不再注入 Legacy Definition。
- Runtime 武器拾取/丢弃闭环：`AShootWeaponPickupActor`（支持 AutoOverlap/PressToInteract）+ `UShootGA_Interaction_Collect` 打通“拾取 → InventoryManager::AddRuntimeWeaponItem → CombatComponent.AssignSlotFromInventory”的流水线；CombatComponent 在移除 RuntimeOnly 槽位时复制 StatTags 并生成拾取 Actor（副本结束通过 `bDisableRuntimeDropSpawn` 抑制刷地），`UShootWeaponInstance::HandleVisualAnimCue` 配合 AnimNotify 控制显隐，旧 GA/UI 继续只与 WeaponInstance/ItemInstance 交互。
- CombatComponent 新增 `DropActiveRuntimeWeapon`（服务器专用），用于丢弃当前 RuntimeOnly 槽位的武器：内部调用现有 `RemoveItemFromSlot` 流程，InventoryManager 移除临时实例并在地面生成 `AShootWeaponPickupActor`。拾取 Actor 默认走 AutoOverlap，还原 StatTag 快照后通过 `GetNextFreeItemSlot → AssignSlotFromInventory` 自动装填并调 `EquipItemInSlot`。
- Quickbar 槽位消息补充 WeaponDefinition 兜底显示，ItemInstance 尚未复制时也能展示武器名称/Icon；当 ItemInstance 到达客户端后自动切换为 Definition Icon/Name 并携带 ItemDefinition Class，HUD 与 Tab 均可直接渲染。
- `UShootQuickbarWidgetBase`/`UShootHUDQuickbarComponent` 接入文档完善：仅需在 PlayerController 蓝图上指定 QuickbarWidgetClass 即可收听 GameplayMessage；Tab/背包可通过 `GetQuickbarSlotsData` 主动刷新，消除旧 Quickbar Actor 依赖。
- 资源面板基类 `UShootResourceListWidgetBase` 支持消息驱动刷新并提供 `RefreshResourceEntries` 主动拉取接口，背包材料标签与 HUD 均可直接继承；文档记录了 Blueprint 接线方式以便设计师落地。
- Phase2 启动：`UCombatComponent` QuickBar 槽位缓存 `UShootWeaponInstance`，新增 `GetActiveWeaponInstance`，并优先使用 UObject 弹药数据（`PopulateQuickbarSlotData`）；旧 Actor 流程仅作表现兼容，GAs 可逐步迁移。
- 武器开火/换弹 GA 与 `UShootAbilityCost_Ammo` 已迁移到 `UShootRangedWeaponInstance`：Ability Spec 的 SourceObject 统一指向 UObject 武器实例，弹药/伤害/投射物配置全部通过 WeaponInstance API 提供，同时 WeaponInstance 负责客户端预测扣弹、逐发换弹，能力代码不再直接依赖 `AShootWeaponActor`。
- Phase2-C 进度：HUD 准星去掉 `AShootWeaponActor` 回退，仅以 `UShootRangedWeaponInstance` 初始化；Quickbar Widget 通过 `Msg_Weapon_AmmoChanged` 刷新弹药，无新 Actor 直连。AnimNotify/GA 特例（Sniper/Shotgun/Projectile）仍保留内部 SpawnedActor 取表现的 TODO，待 Phase2-C 收尾。
- 阅读 GameDesign v7.0 材料/制作章节并提炼到 `References/GameDesign_MaterialsAndCrafting.md`
- 梳理现状与痛点，完成 Context/Requirements/DesignSpec/Implementation/Checklist 全套文档
- 给出 Lyra 库存系统移植方案（ShootInventory* 类、阶段划分、对 CombatComponent 的衔接策略）
- Phase 0 清理：InventoryManagerComponent 移至 `AShootPlayerState` 并改为通过 Character Getter 间接访问，确保库存与账号层绑定，会议记录已更新
- 新增 `UResourceInventoryComponent`（PlayerState）：提供 Add/Consume/Query 资源接口与资源变更消息 Tag，支持材料/货币/徽章/设计图账号层管理
- 将 “ResourceInventory = 数量型仓库 / InventoryManager = 有身份背包 / QuickBar 仅引用 InventoryManager” 的架构约束写入 AGENTS 与任务文档
- 定义 Persistent vs RuntimeOnly 武器生命周期规则，要求 InventoryManager 提供 AddPersistentItem/AddRuntimeItem，并在文档/Checklist 中建立验收项
- 追加代码规范：库存/武器相关 C++ 必须带中文注释说明上述层级，防止 AI 同事遗忘
- 交互 GA (`UShootGA_Interact`/`UShootGA_Interaction_Collect`) 与拾取枚举 C++ 落地，同时补充 `Ability.ActivateFail.Cost` GameplayTag 及 Iris 复制依赖说明
- Phase 1 资源接口迭代：`UResourceInventoryComponent`/BlueprintLibrary 新增 `GetAllResources`、`HasEnoughResource`，并在 `BroadcastChange` 中追加 HUD Toast Tag（`UI.Toast.ResourcePickup`）；文档同步记录资源/背包责任划分
- QuickBar/UCombatComponent/ItemLifetime 行动计划以 Implementation Guide + DevelopmentLog 形式落地，包含槽位引用 ItemInstance、AddPersistentItem/AddRuntimeItem、SaveGame 过滤、AShootWeaponActor/AHitscan 技术债拆解顺序
- HUD 资源 Toast 流程落地：`AShootPlayerController` 默认挂载 `UShootHUDResourceToastComponent`，监听 `UI.Toast.ResourcePickup` 并驱动 `UShootResourceToastWidgetBase`（蓝图可实现视觉）；仅本地 PlayerController 生效，遵循“ResourceInventory=数量仓库”注解
- CommonUI 约定落地：Implementation Guide 记录 Activate/Deactivate、Layer/Stack、HUD 组件模式与交互提示解耦方案；新增 QuickBar 槽位→ItemInstance PoC 与 ItemLifetime API 细化，为后续编码提供明确信号
- QuickBar 槽位复制结构更新：`FShootQuickbarSlot` 与 `FQuickbarSlotData` 均包含 `ItemInstanceId`/`EShootItemLifetime`，CombatComponent 的 `AddItemToSlot` 可接收 `UShootInventoryItemInstance`；UI 现可分辨 Persistent/RuntimeOnly，下一步按计划把装备流程接入 EquipmentManager
- CombatComponent 提供 `AssignSlotFromInventory`/`ReleaseSlot` API，Slot 绑定由 Inventory Guid 驱动；当 ItemDef 带 Equippable Fragment 时会自动通过 `UShootEquipmentManagerComponent` 装备并缓存 EquipmentInstance，剩余 Actor 逻辑仅作兼容
- SaveGame 接口第一次落地：`FSavedInventoryItem/FSavedQuickbarSlot` 记录物品与槽位 Guid，InventoryManager/CombatComponent 提供 Build/Apply API；`USaveGameSubsystem::Capture/RestorePlayerInventoryState` 统一调用，支持 Hub/关卡切换前保存 Persistent 项并清理 Runtime 槽位
- CombatComponent `ClearRuntimeSlots` 现在会同步调用 `InventoryManager->RemoveRuntimeItems()` 并经由 `GetInventoryManagerForOwner()` 维持 “QuickBar 仅引用 PlayerState.InventoryManager” 约束，避免 RuntimeOnly 实例残留
- `AShootPlayerState` 在 `BeginPlay/EndPlay` 自动调用 `USaveGameSubsystem::Restore/CapturePlayerInventoryState`，确保 Hub/副本进入退出都会落盘

Phase2 总结（架构总览）：
- PlayerState：`UShootInventoryManagerComponent`（有身份背包：Persistent/RuntimeOnly）+ `UResourceInventoryComponent`（数量型资源），SaveGame 仅序列化 Persistent。
- Character：`UCombatComponent` 维护 QuickBar 槽位 Guid/Lifetime，`GetOrCreateWeaponInstanceFromInventory` 构建 `UShootWeaponInstance`，Equip/Unequip 只依赖 UObject；Legacy `AShootWeaponActor/AShootWeaponActor` 仅在 `GetSpawnedActors()` 内部作为表现壳。
- Ability/UI：`UShootWeaponInstance/UShootRangedWeaponInstance` 是对外唯一武器接口，GA/HUD/Reticle/AnimNotify 通过 SourceObject 或 CombatComponent 获取 WeaponInstance；需要特效/Attach 时仅在内部 Cast SpawnedActors，不再向外暴露 Actor。
- 拾取/丢弃：`AShootWeaponPickupActor` 负责 RuntimeOnly 落地与 AutoEquip，`DropActiveRuntimeWeapon` + `ClearRuntimeSlots` 确保副本临时物品不写存档；ResourcePickup 仍只走 ResourceInventory。

下一步（Phase5 规划）：
- 评估是否合并 `AShootWeaponActor` 为单一 `AShootWeaponActor` 并执行 Blueprint 重新挂载。
- 深化资源/TagStack（弹药/制作/补给）自动化测试与覆盖，保持 SaveGame/公开 BP API 稳定。

Phase3 进度（done）：
- 约束落地：在 DesignSpec 声明 “Instance=UObject，Actor=视觉壳”，禁止新增对外 Actor API；记录 Phase4 重命名预案（AShootWeaponActor → AShootWeaponActor 等，暂不执行）。
- Ammo 成本链路确认：`UShootAbilityCost_AmmoTagStack`（继承 ItemTagStack，替代旧 UShootAbilityCost_Ammo）/`UShootRangedWeaponInstance` 以 StatTags 扣弹，服务器权威扣减，客户端仅预测激活，未触碰 Actor 字段。Self-check: 仅更新描述，与当前代码一致。
- GA 表现收口：`ShootGA_Weapon_Fire_Sniper/Shotgun/Rifle/Projectile` 的 GameplayCue `SourceObject` 统一为 WeaponInstance，Actor 仅在内部用于表现挂点。
- 投射物链路：`AShootProjectileBase` 现在仅缓存 `UShootRangedWeaponInstance`；`ShootGA_Weapon_Fire_Projectile` 初始化投射物传入 WeaponInstance，SpawnedActor 仅作可选视觉壳。
- 弹药补给辅助：`UShootRangedWeaponInstance` 新增 `AddReserveAmmo/AddMagazineAmmo`（服务器），`UCombatComponent::GrantAmmoToActiveWeapon` 提供当前槽位补给入口，供拾取/副本补给后续调用。
- ResourcePickup 弹药路径：新增 `bGrantAmmoToActiveWeapon`（Magazine/Reserve Delta），拾取时直接调用 CombatComponent 补给当前武器的 StatTags（RuntimeOnly 弹药不入存档），否则走 ResourceInventory 流程。

Phase3 完成概述：
- 架构：PlayerState.InventoryManager/ResourceInventory → Character.CombatComponent → QuickBar → UShootWeaponInstance/UShootRangedWeaponInstance → GA/HUD/AnimNotify/Projectile；Actor 仅作为 SpawnedActors 表现壳。
- 弹药/成本：Check/ApplyCost 与补给全部走 StatTags（`Inventory_Ammo_Magazine/Reserve`）；副本拾取弹药通过 ResourcePickup 弹药模式写入 TagStack，RuntimeOnly 不入存档；ResourceInventory 仍负责数量型资源。
- 对外接口：所有公开 API/GameplayCue SourceObject 均使用 WeaponInstance（UObject），禁止新增 Actor 对外暴露；Phase4 计划仅做命名重定向（AShootWeaponActor→AShootWeaponActor 等）。
- 测试建议：需在单机与 Listen Server 手工验证弹药拾取补给、GA 开火/换弹消息、AnimNotify 显隐和 HUD/QuickBar 更新；目前未执行自动化测试。

Phase4 完成概述：
- 类重命名：`AShootWeaponActor`（原 ARangedWeaponInstance）、`AShootWeaponActor`（原 AHitscanWeaponInstance），无 Class Redirect；蓝图需手动重绑。
- 职责分层：InventoryItemDefinition=物品元数据，WeaponDefinition=战斗调参 DataAsset，WeaponInstance=运行时逻辑与 TagStack 弹药，WeaponActor=纯表现壳（仅 SpawnedActors 内部访问）。
- 对外接口：GA/HUD/Reticle/AnimNotify/GameplayCue 只暴露 WeaponInstance/Inventory；Actor 触点仅限 CombatComponent/内部 helper 控制 Mesh/FX/可见性。

遇到问题：
- 部分协作者只能读取 Docs/ExampleProjectCode 目录，需要我们提前整理上下文（已完成）

相关Commit：
- （当前更改尚未提交代码，等待实现阶段统一提交）
Phase5（进行中，破坏性重构：删除 WeaponDefinition 家族，全面并入 Fragment）
- Step0 引用扫描：当前仍引用 UWeaponDefinition/子类的主要位置：
  * 类文件：Weapons/WeaponDefinition.h、HitscanWeaponDefinition.h、ProjectileWeaponDefinition.h、RifleWeaponDefinition.h、SniperWeaponDefinition.h、GrenadeLauncherWeaponDefinition.h、RocketLauncherWeaponDefinition.h。
  * 使用点：ShootRangedWeaponInstance(获取/设置 WeaponDefinition，射击参数)、ShootHUDReticleComponent(取 ReticleWidgetClass)、CombatComponent(Quickbar 填充 Icon/WeaponId)、ShootEquipmentManagerComponent(ResolveWeaponDefinition/注入)、ShootInventoryItemDefinition_Weapon/EquipmentDefinition 挂 Legacy WeaponDefinition 指针、文档与拾取路径仍描述 WeaponDefinition。
- 破坏性约束：接受旧存档/蓝图失效，不做兼容层；禁止回退到 Actor/Definition 弹药路径。
- 下一步（Step1）：新增 Fragment 组（WeaponBasicConfig / RangedWeaponConfig / ProjectileWeaponConfig），至少迁移一把武器配置，并改 WeaponInstance/GA/Projectile 取值路径为 Fragment；随后逐步删除 WeaponDefinition 类型。
